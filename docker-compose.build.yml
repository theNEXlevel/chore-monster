# Adds the `build:` key that docker-compose.yml omits, for building locally:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml --profile production up --build -d
services:
  chore-monster-app:
    build:
      context: .
      dockerfile: Dockerfile
